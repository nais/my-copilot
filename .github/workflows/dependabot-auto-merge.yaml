name: Dependabot
on: pull_request

permissions:
  pull-requests: write

jobs:
  auto-merge:
    uses: nais/actions/.github/workflows/dependabot-auto-merge.yaml@main
    secrets:
      DEPENDABOT_AUTO_MERGE_APP_ID: ${{ secrets.DEPENDABOT_AUTO_MERGE_APP_ID }}
      DEPENDABOT_AUTO_MERGE_APP_SECRET: ${{ secrets.DEPENDABOT_AUTO_MERGE_APP_SECRET }}
